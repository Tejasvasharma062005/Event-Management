{
  "name": "event-management",
  "version": "1.0.0",
  "description": "Event Management System with AI Recommendation Engine",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "build:frontend": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
